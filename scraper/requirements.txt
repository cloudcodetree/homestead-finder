# Web scraping
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2
scrapy==2.11.2
selenium==4.21.0
webdriver-manager==4.0.1
playwright==1.52.0
# Stealth patches for Playwright — hides navigator.webdriver +
# language/plugin/hairline signals that Cloudflare anti-bot uses.
# Lets local Playwright replace Firecrawl for JS-rendered sites.
playwright-stealth==2.0.3
# Chrome TLS-fingerprint impersonation — passes Cloudflare WAF walls
# on LandWatch / LOA / Bid4Assets for $0. See
# scraper/strategies/curl_cffi_strategy.py for the rationale.
curl-cffi==0.13.0

# AI-powered fallback strategies
firecrawl-py==1.5.0
anthropic>=0.40.0

# Data processing
python-dateutil==2.9.0
pdfplumber==0.11.4

# Email notifications
sendgrid==6.11.0

# Linting / formatting (dev)
ruff==0.4.9

# Testing
pytest==8.2.2
pytest-cov==5.0.0
responses==0.25.3

# Utilities
python-dotenv==1.2.2
# seeds.yml parsing for the source-discovery pipeline
PyYAML>=6.0

# Personalization — logistic regression via scipy L-BFGS-B. ~80MB
# install but widely cached on GitHub runners. Used by rank_fit.py
# (nightly per-user model fit).
numpy>=1.26
scipy>=1.11

# Project-files text extraction (project_files_extract.py).
# python-docx for .docx, openpyxl for .xlsx — pdfplumber + pandas
# are already pulled in for tax-sale parsing. Each ~1MB.
python-docx>=1.1
openpyxl>=3.1
